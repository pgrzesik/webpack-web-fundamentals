{
  "scripts": {
    "build:log": "yarn build -- --env=1",
    "build:debug": "yarn build -- --env.debug=1",
    "build:watch": "yarn build -- --watch",
    "build": "webpack"
  },
  "devDependencies": {
    "webpack": "^3.3.0",
    "webpack-dev-server": "^2.6.1"
  }
}
