{
  "scripts": {
    "build:watch:dev": "yarn build:watch -- --env.env=dev",
    "build:watch": "yarn build -- --watch",
    "build": "webpack"
  },
  "devDependencies": {
    "webpack": "^3.3.0",
    "webpack-dev-server": "^2.6.1",
    "webpack-merge": "^4.1.0"
  }
}
